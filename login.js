const form=document.getElementById('loginForm'),message=document.getElementById('loginMessage');form?.addEventListener('submit',e=>{e.preventDefault();const phone=document.getElementById('loginPhone').value.trim().replace(/\s+/g,'').replace(/^\+260/,'0'),client=JSON.parse(sessionStorage.getItem('client')||'null');if(!client){message.textContent='No customer session exists in this browser. Complete signup first.';message.className='form-message show';return;}if(phone!==client.phone){message.textContent='That phone number does not match this browser session.';message.className='form-message show';return;}sessionStorage.setItem('customerLoggedIn','true');location.href='dashboard.html'});
