const form=document.getElementById('adminLoginForm'),message=document.getElementById('adminLoginMessage');form?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('adminName').value.trim();if(!name){message.textContent='Enter an admin name.';message.className='form-message show';return;}sessionStorage.setItem('adminLoggedIn','true');sessionStorage.setItem('adminName',name);location.href='admin.html'});
